{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "msedge",
			"request": "launch",
			"name": "Launch MSEdge for debugging",
			"url": "http://localhost:5173",
			"webRoot": "${workspaceFolder}"
		}
	]
}
